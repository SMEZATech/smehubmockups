# Local tooling / preview server config (not part of the mockups)
.claude/

# OS cruft
Thumbs.db
desktop.ini
.DS_Store

# Editor
.vscode/
*.swp
*~

# Office temp lock files
~$*

# Excluded from the public mockup site — kept locally only, not published
social_media_studio.html
Startup_Africa_Newsletter_Comparison.html

# Internal working docs — kept locally, not published
AGENT-HANDOVER.md
sme-hub-project-brief.md

# Private status controller dashboard — local only, never published
control.html

# Secrets — never commit anything that smells like a token
*token*.txt
*.token
.env
.env.*

# Original old-brand podcast design — kept locally for reference, not published
podcast_original/
